[pytest]
pythonpath = src
# 収集対象は tests/ だけに限る。docs/ は .gitignore 済みのローカル作業用置き場で、
# 過去の検証用スクリプト（docs/test_pr16_review_fixes.py 等）が残っていると
# 引数なしの `pytest` がそれも拾ってしまう。リポジトリに無いファイルが
# セッション全体のグローバル状態（utils の debug_log_enabled など）を書き換え、
# 本来のテストの結果や終了コードに影響していた。
testpaths = tests
